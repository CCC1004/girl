# 本地环境prod
spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bootdb001?characterEncoding=utf8&useSSL=false&serverTimezone=UTC
    username: boot
    password: boot

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true




